{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build": "npm run build --workspaces --if-present && tsc",
    "lint": "oxlint && prettier --check .",
    "format": "prettier --write .",
    "fixtures:check": "npm run build --workspace @slackwater/engine && node --test fixtures/generate/write.test.mjs && node fixtures/generate/gen-golden.mjs --check && node fixtures/generate/gen-catalog.mjs --check && node fixtures/generate/gen-realworld.mjs --check && node fixtures/generate/gen-subordinates.mjs --check",
    "benchmarks": "run-s --continue-on-error \"benchmarks:*\"",
    "benchmarks:noaa": "node benchmarks/noaa.ts",
    "benchmarks:chs": "node benchmarks/chs.ts",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bash scripts/release.sh"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.29.8",
    "@types/make-fetch-happen": "^10.0.4",
    "@types/node": "^26.0.1",
    "@vitest/coverage-v8": "^5.0.1",
    "make-fetch-happen": "^16.0.0",
    "npm-run-all": "^4.1.5",
    "oxlint": "^1.74.0",
    "pkg-pr-new": "^0.0.88",
    "prettier": "3.9.7",
    "tsdown": "^0.23.0",
    "typescript": "^7.0.2",
    "vitest": "^4.0.15"
  },
  "workspaces": [
    "packages/engine",
    "packages/slackwater",
    "packages/api",
    "packages/cli",
    "packages/react"
  ]
}
