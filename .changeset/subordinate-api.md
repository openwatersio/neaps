---
"@neaps/api": minor
---

Add support for subordinate station predictions in the API.

`GET /tides/timeline` and `GET /tides/waterlevel` now return predictions for subordinate stations instead of a 400 error.
